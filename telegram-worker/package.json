{
  "name": "telegram-pixel-dungeon-bot-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "dev": "npx wrangler@latest dev",
    "deploy": "npx wrangler@latest deploy",
    "register-webhook": "node register-webhook.mjs"
  }
}
